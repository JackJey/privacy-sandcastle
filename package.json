{
  "name": "privacy-sandcastle",
  "version": "0.0.0",
  "description": "Privacy Sandcastle: Mixed demo of Privacy Sandbox APIs",
  "scripts": {
    "start": "cd docker-compose/.devcontainer && docker-compose build && docker-compose up"
  }
}
