{
  "name": "privacy-sandcastle",
  "version": "0.0.0",
  "license": "apache-2.0",
  "description": "Privacy Sandcastle: Mixed demo of Privacy Sandbox APIs",
  "scripts": {
    "start": "npm run cert && npm run docker",
    "cert": "cd docker-compose/nginx/cert && ./mkcert.sh",
    "build": "cd docker-compose && docker-compose build",
    "docker": "cd docker-compose && docker-compose build && docker-compose up",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "firebase-tools": "^11.20.0"
  }
}
